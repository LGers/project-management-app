import { styled } from '@mui/system';
import { Card } from '@mui/material';

export const ColumnCard = styled(Card)({
  minWidth: 275,
  width: 275,
  mr: 1,
  height: '100%',
  overflowY: 'auto',
  overflowX: 'hidden',
});
